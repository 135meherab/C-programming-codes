#include <stdio.h>
int char_to_ascii(char caracter)
{
    char ascii;
    if ( caracter>='A' && caracter <= 'z')
    {
        ascii = caracter;
    }
    else if (caracter >= 'a' && caracter <= 'z')
    {
        ascii = caracter
    }
    return ascii;
    
}
int main()
{
    int 
    return 0;
}